import { Header, Footer, CTA } from '../../components';
import { serviceDetails, services, site } from '../../data';
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);return {title:s?.title||'Service',description:s?.desc}}
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug)||services[0];const detail=serviceDetails[s.slug];return <><Header/><main><section className='service-hero'><div className='container two'><div><p className='eyebrow'>Filipino assistant service</p><h1>{s.title}</h1><p className='lead'>{s.desc}</p><a className='btn' href='/contact'>Plan this role</a></div><div className='hero-card'><img src={site.serviceImage} alt={`Filipino assistant team providing ${s.title.toLowerCase()}`}/></div></div></section><section className='section'><div className='container cards'><div className='card'><h3>Work to hand off</h3><ul>{detail.tasks.map((item)=><li key={item}>{item}</li>)}</ul></div><div className='card'><h3>Checks to keep</h3><ul>{detail.controls.map((item)=><li key={item}>{item}</li>)}</ul></div><div className='card'><h3>Start the first week</h3><ul>{detail.firstWeek.map((item)=><li key={item}>{item}</li>)}</ul></div></div></section><CTA/></main><Footer/></>}
