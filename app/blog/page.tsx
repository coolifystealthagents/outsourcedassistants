import { Header, Footer } from '../components';import { blogPosts, site } from '../data';export default function Blog(){return <><Header/><main className='section'><div className='container'><p className='eyebrow'>{site.brand}</p><h1>Guides to hiring Filipino assistants</h1><p className='lead'>Practical notes on role planning, hiring, onboarding, and provider calls.</p><div className='cards'>{blogPosts.map(p=><a className='card' href={`/blog/${p.slug}`} key={p.slug}><h3>{p.title}</h3><p>{p.excerpt}</p><span className='pill'>{p.minutes} min read</span></a>)}</div></div></main><Footer/></>}
