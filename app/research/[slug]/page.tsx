import {notFound} from 'next/navigation';import {Header,Footer,CTA} from '../../components';import {researchPosts} from '../../data';export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}export default async function ResearchPost({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);if(!p)notFound();return <><Header/><main className="section"><article className="container guide-article"><p className="eyebrow">Philippines staffing research</p><h1>{p.title}</h1><p className="lead">{p.excerpt}</p><img src={p.image} alt="Research team reviewing support operations" width="1200" height="675" /><div className="card">{p.body.map(x=><p key={x}>{x}</p>)}<p>Sources: {p.citations.map(c=><a href={c} key={c}>{c}</a>)}</p></div></article><CTA/></main><Footer/></>}
